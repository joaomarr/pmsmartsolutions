import { Contatos } from "./Contatos";
import { Map } from "./Map";

export function MainContato() {
    return (
        <main className="relative">
            <Map />
            <Contatos />
        </main>
    )
}